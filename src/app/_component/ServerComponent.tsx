// Create Server React Component with Nextjs
import React from "react";

const ServerComponent: React.FC = () => {
  return (
    <div>
      Server Component
    </div>
  );
};

export default ServerComponent;